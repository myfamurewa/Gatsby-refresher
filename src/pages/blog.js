import React from "react"
import { graphql } from "gatsby"
import Hero from "../components/hero"
import Layout from "../components/layout"

const Blog = ({ data }) => {
  return (
    <Layout>
     My blogs would go here if I had any 
    </Layout>
  )
}

export default Blog